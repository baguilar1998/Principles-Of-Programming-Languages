
public abstract class Statement {
	void printParseTree(String indent) {
		IO.displayln(indent + indent.length() + " <statement>");
		System.out.println(indent + indent.length() + " <statement>");
	}
}
