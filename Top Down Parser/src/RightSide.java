
public abstract class RightSide {
	void printParseTree(String indent) {
		IO.displayln(indent + indent.length() + " <right side>");
		System.out.println(indent + indent.length() + " <right side>");
	}
}
